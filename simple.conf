
server {
    listen          0.0.0.0:80;
    server_name     localhost;

    index   index.html;
    autoindex on;

    location / {
        index   index.html;
        client_max_body_size 100;
        error_page 404 /404.html;
        root resources/html/page;
    }

    location /autoindex/ {
        root resources/html/autoindex_demo;
    }

    location /coolpage/ {
        index   index.html;
        root resources/html/coolpage;
    }

    location /redirect/ {
        return 308 http://localhost:80/coolpage/;
        index   index.html;
        root resources/html/coolpage;
    }

    location /page {
        index   index.html;
        root resources/html;
    }

    location /upload/ {
        upload_directory resources/upload;
    }

    location /del/ {
        root resources/html/demo_del;
    }

    location /cgi/ {
        cgi_path resources/cgi-bin/cgi_tester.cgi;
        cgi_extension bla;
        root resources/html/cgi_demo;
    }
}
